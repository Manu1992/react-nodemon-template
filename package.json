{
  "name": "react-nodemon-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npm-run-all --parallel client server",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "install": "npm --prefix ./client install ./client && npm --prefix ./server install ./server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^3.1.1"
  }
}
